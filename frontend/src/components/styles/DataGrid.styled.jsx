import styled from "styled-components";

const DataGridStyle = styled.div`
  table {
    thead {
      tr {
        th {
          background: #4a2569;
          color: #ffffff;
        }

        div span svg {
          color: #ffffff;
        }
      }
    }
  }
`;

export default DataGridStyle;
