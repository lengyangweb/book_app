import styled from "styled-components";

const HeaderContainer = styled.header`
  background: #491974;
  // border-bottom: 2px solid #491974;
  box-shadow: 0 0 3px;
`;

export default HeaderContainer;
